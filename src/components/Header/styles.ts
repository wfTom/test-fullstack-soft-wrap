import styled from 'styled-components'

export const Container = styled.div`
  display: flex;
  width: 100vw;
  height: 4.3rem;
  background-color: var(--white);
  font-family: 'Inter';
  font-weight: bold;
  font-size: 2rem;
  align-items: center;

  > div {
    width: 63%;
    display: flex;
    margin: auto;
  }
`
